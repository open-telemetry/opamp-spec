# buf.yaml
version: v1
name: buf.build/open-telemetry/opamp-spec
lint:
  use:
    - STANDARD
  except:
    - ENUM_ZERO_VALUE_SUFFIX
    - ENUM_VALUE_PREFIX
    - ENUM_VALUE_UPPER_SNAKE_CASE
    - ONEOF_LOWER_SNAKE_CASE